### Ruby on Rails API Endpoint

POST /api/v1/user/

This endpoint is used for creating a new user (aka. signing up).
It requires `firstName`, `lastName`, `email`, and `password` to
be passed as POST parameters.
